# Domain Presets · Metadata Lookup

This sample inspects presets without instantiating the generator. Use it to build CLIs or governance bots that need schema metadata.

## Run

```bash
node code_samples/domain-presets/list-metadata/sample.js
```

## What it demonstrates

- Listing all preset keys.
- Fetching configuration + metadata for a single preset (`device`).
- Printing compartment definitions for documentation purposes.

