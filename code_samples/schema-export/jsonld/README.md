# Schema Export · JSON-LD

Builds the JSON-LD schema artifact for the `person` preset and prints the compartments so you can push them into a graph store or documentation site.

## Run

```bash
node code_samples/schema-export/jsonld/sample.js
```

## What it demonstrates

- `buildSchemaForPreset('person')`
- Accessing `jsonld.sig:compartments` for downstream tooling

