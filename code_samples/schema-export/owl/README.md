# Schema Export · OWL

Shows how to emit the RDF/OWL version of a preset schema. Ideal for feeding Neo4j, Neptune, Blazegraph, or any RDF-capable system.

## Run

```bash
node code_samples/schema-export/owl/sample.js
```

## What it demonstrates

- Calling `exportSchema('dataset', 'owl')`
- Previewing the first few lines of the RDF/XML payload

